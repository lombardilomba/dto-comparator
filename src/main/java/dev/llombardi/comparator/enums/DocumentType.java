package dev.llombardi.comparator.enums;

/**
 * Enumeration of document types.
 * Used as an example in DTOs to demonstrate comparison of enumerated types.
 */
public enum DocumentType {
    TYPE1,
    TYPE2
}
